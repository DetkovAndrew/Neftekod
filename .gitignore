# Raw hackathon data — proprietary Gazprom Neft telemetry, never commit
#
# ВАЖНО: /data/ и /raw/ здесь ЯКОРНЫЕ (ведущий слэш) — без него
# `data/` матчит ЛЮБУЮ директорию с этим именем на любой глубине
# дерева, включая src/neftekod_mas/data/ (реальный баг, обнаруженный
# и исправленный: loaders.py/sync.py никогда не попадали в git из-за
# этого якоря — см. git log).
*.csv
*.xlsx
*.xls
*.rar
*.zip
*.pdf
/data/
/raw/
/neftekod/
runs/

# Скриншоты дашборда: на них видны реальные лабораторные значения
# (сера, T95, цетановое число) с временными метками по сотням точек --
# это те же данные, из-за которых в git не попадают runs/ и сами CSV.
# Репозиторий публичный, поэтому скриншоты остаются локальными: они
# генерируются командой `python scripts/capture_dashboard.py` за минуту.
docs/screenshots/
config/joint_envelope.npz

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# OS / editor
.DS_Store
*.swp

# Local env
.env
.venv-linux/
.idea/
